# i-js 🚀

Uma biblioteca leve e eficiente para simplificar o desenvolvimento JavaScript, focada em lógica condicional e utilitários de validação.

## 📌 Sobre o Projeto

O **i-js** demonstra a aplicação de boas práticas em JavaScript moderno, como o uso de operadores ternários para código limpo e interrupções lógicas baseadas em condições específicas.

## 🚀 Funcionalidades

- ⚡ **Código Limpo:** Uso extensivo de operadores ternários.
- 🛡 **Validação Segura:** Sistema de interrupção para valores proibidos.
- 📦 **Estrutura Profissional:** Preparado para automação com Makefile.

## 🛠 Comandos de Desenvolvimento (Makefile)

Utiliza o `Makefile` para gerir o ciclo de vida do projeto:

| Comando         | Descrição                                             |
|:----------------|:------------------------------------------------------|
| `make install`  | Instala as dependências (npm install).                |
| `make run`      | Executa o script principal (`src/index.js`).          |
| `make test`     | Executa a suite de testes unitários.                  |
| `make clean`    | Limpa pastas temporárias e node_modules.              |

## 📦 Instalação e Uso

Podes clonar o repositório:

```bash
git clone [https://github.com/RodrigoDias123/i-js.git](https://github.com/RodrigoDias123/i-js.git)
cd i-js
make install
make run